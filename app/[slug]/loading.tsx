import MovieDetailsk from "@/components/Skeletons/detailsskeleton/MovieDetails";
import React from "react";

type Props = {};

const loading = (props: Props) => {
  return <MovieDetailsk />;
};

export default loading;
